

	Due date: February 28, 2023

Tips:
	- look in /var/log/syslog and use grep to find the printk's from
	the inserted modules.

	- To get into the real, non-emulated terminal, press Ctrl + Alt + <F3> (or any of the 
	F keys). Can be used to see KERN_ALERT outputs.
